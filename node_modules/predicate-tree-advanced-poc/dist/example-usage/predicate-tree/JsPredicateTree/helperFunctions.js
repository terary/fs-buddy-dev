"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.predicateOperatorToJsOperator = exports.predicateJunctionToJsOperator = exports.quoteValue = void 0;
const predicateOperatorToJsOperator = (operator) => {
    switch (operator) {
        case "$eq":
            return "===";
        case "$gt":
            return ">";
        case "$gte":
            return ">=";
        case "$lt":
            return "<";
        case "$lte":
            return "<=";
        default:
            return operator;
    }
};
exports.predicateOperatorToJsOperator = predicateOperatorToJsOperator;
const predicateJunctionToJsOperator = (operator) => {
    switch (operator) {
        case "$and":
            return "&&";
        case "$or":
            return "||";
        default:
            // @ts-ignore - $addressTree not assignable to && or ||
            return operator;
    }
};
exports.predicateJunctionToJsOperator = predicateJunctionToJsOperator;
const quoteValue = (datatype, value) => {
    switch (datatype) {
        case "string":
        case "datetime":
            return `'${value}'`;
        case "number":
            return value;
        default:
            break;
    }
};
exports.quoteValue = quoteValue;
//# sourceMappingURL=helperFunctions.js.map